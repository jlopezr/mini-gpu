# Camino básico de ejecución

| Caso | Qué valida |
|---|---|
| [smoke](smoke/) | `MOVI`, `ADD`, `STORE`, `LOAD` y `HALT` con el mapa de memoria global |

Es el caso mínimo para comprobar que un backend nuevo (o una FPGA recién
programada) responde antes de ejecutar nada más largo.
