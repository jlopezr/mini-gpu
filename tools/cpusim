#!/usr/bin/env python3
import os
import runpy
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]

if __name__ == "__main__":
    source = ROOT / "2.cpu-sim-func" / "minicpu_sim.py"
    if not source.exists():
        print(f"error: no existe {source}", file=sys.stderr)
        raise SystemExit(2)
    sys.path.insert(0, str(ROOT / "2.cpu-sim-func"))
    os.environ.setdefault("PYTHONPATH", str(ROOT / "2.cpu-sim-func"))
    runpy.run_path(str(source), run_name="__main__")
